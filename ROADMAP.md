# ROADMAP — DimArch Sage v2

Миграция компонентов на палитру `colors/palette.json` v2.0.0.

`palette.json` — источник истины. Правки цвета начинаются там, дальше расходятся
по компонентам. Порядок шагов не случаен: сначала проверяются решения, которые
влияют на всё остальное, и только потом идёт массовая раскатка.

---

## Шаг 0 — фундамент ✅

- [x] Аудит ~20 конфигов в `dimarch`, найдено три параллельных подпалитры
- [x] Устранены четыре цвета-сироты: `#2f7d68`, `#d65d5d`, `#7c8cd8`, `#c8cad4`
- [x] Обезврежен конфликт `colors.lua` ↔ `decoration.lua` за один ключ
- [x] Исправлены комментарии, вводившие в заблуждение (swaync, swayimg, uosc, yazi)
- [x] `palette.json` v2 — ramp + role + surface + ANSI, 22 токена и 63 роли
- [x] `sage-dark.css`, `sage-light.css` — токены обоих слоёв
- [x] Светлый ANSI 16 и bright-ярус светлого ramp

---

## Шаг 1 — решения, влияющие на всё ✅

### 1.1 Фон 180° ✅

Все поверхности сведены к одному хюи: chrome `13,25,25`, terminal `#171b1b`.
Производные пересчитаны с сохранением смещений от базы.

Сдвиг оказался **невидимым** при прямом сравнении — принят ради одного
канонического значения, а не ради вида. Прошлый откат (`#181e1a`) менял хюи,
светлоту и насыщенность одновременно, поэтому чистый сдвиг хюи не тестировался
ни разу. Контраст 12.67 → 12.49.

### 1.2 ANSI blue → лишайник ✅

Принят и подтверждён живым просмотром. До этого blue `#6ba88c` и green `#7fb89e`
сидели на одном хюи (152° и 153°) — терминал имел два слота, несущих один цвет,
директории не отделялись от исполняемых файлов. Лишайник даёт разнос 65°, не
вводя холодного оттенка.

---

## Шаг 2 — линтер

- [ ] `tools/lint-palette.py` — сверяет каждый hex в `dimarch` со значениями палитры
- [ ] Понимает все нотации: `#rrggbb`, `rrggbb`, `#RRGGBB`, `rgba(r,g,b,a)`,
      `rgba(rrggbbaa)`, `0xAARRGGBB`, десятичный `rgba(127,184,158,0.35)`
- [ ] Отличает живое значение от упоминания в комментарии
- [ ] Репортит: чужой цвет, файл, строка, ближайший токен палитры
- [ ] Игнор-лист для законных исключений (вендорные темы, per-name иконки)

Приоритет вырос по итогам раскатки: три пропуска нашлись вручную и случайно —
`screenshot.sh` (сирота вне аудита), статусные строки zathura (мигрирован был
только `default-bg`), цвета иконок yazi (вкомпилированы в бинарник, секции
`[icon]` в теме не было вовсе). Линтер ловит ровно этот класс.

Почему линтер, а не генератор: правило «palette.json — source of truth»
существовало и не работало, потому что файл физически ни к чему не подключён.
Линтер стоит вечер и не мешает подбирать цвета глазами. Генератор имеет смысл
собирать, когда палитра устоится.

---

## Шаг 3 — раскатка по компонентам

**Компонент считается мигрированным только когда он внесён в
`colors/components.json`.** Карта отвечает на вопрос, который возникает первым при
правке конфига — «что брать и куда класть», — и работает ДО правки, в отличие от
линтера. Она же хранит `_findings`: какие ключи компонент реально принимает и какие
из них мертвы. Это знание теряется так же легко, как значение цвета.

Компоненты, мигрированные до появления карты (шаг 3.1 и часть 3.2), нужно внести
в неё задним числом — иначе проверить их нечем.

### 3.1 Терминальная группа ✅

- [x] `ghostty/config.ghostty` — ANSI 16, фон `#171b1b`, cursor, selection
- [x] `ghostty/light.ghostty` — светлый ANSI 16 и поверхности целиком
- [x] `yazi/theme.toml` — типы файлов, git, режимы, маркеры **и цвета иконок**
- [x] `starship.toml` — палитра переписана, 7 мёртвых токенов убраны
- [x] `uosc.conf`, `swayimg/init.lua`, `mpv.conf` — поверхности

### 3.2 Панель и chrome — частично

- [x] Фоны: waybar, swaync, rofi, swayosd, zathura
- [x] `zathura` — строки статуса, ввода и автодополнения
- [ ] `waybar/style.css` — точечные акценты на модулях (погода → патина,
      звук → вереск, VPN → шалфей, caffeine → лишайник)
- [ ] `waybar/config-top.jsonc` — inline-span цвета иконок (`#6ba88c` остаётся)
- [ ] `swaync/style.css` — уведомления на `sage.bright`
- [ ] `rofi/theme.rasi` — убрать 10 захардкоженных `rgba` мимо собственных токенов
- [ ] `hyprlock.conf` — решить судьбу белого текста (пока оставлен намеренно)

### 3.3 Приложения

- [x] `herdr/config.toml` — **пилот карты компонентов**. 15 ключей по карте,
      мёртвые `surface0`/`surface1` удалены. Вскрыл два пробела в палитре:
      `ramp.sage.shade` (пятая ступень глубины) и семейство `ramp.stone`
      (нейтраль с sage-биасом). `herdr config check: ok`
- [ ] `vscode/settings.json` — три слоя фона, акцент, статусная строка
      (15 значений вне палитры)
- [ ] `obsidian/snippets/sage.css` — **ссылки на `#1f7957` / `#1c6f50`**
      (текущие дают 2.74:1 — единственный найденный баг доступности)

### 3.4 `dimarch-taskbar` — отдельный репозиторий

Нижняя панель — это **не второй waybar**, а самостоятельное приложение
(Python + GTK4, репозиторий `dmitrax/dimarch-taskbar`). Waybar запускает один
экземпляр из `config-top.jsonc`; правила `window#waybar.bottom` в его стилях
мёртвые и помечены как таковые.

- [ ] Подключить палитру: GTK4 читает `:root` custom properties, то есть
      `sage-dark.css` можно подключить напрямую через `@import`
- [ ] Решить хюи фона: `chrome.panel_bottom` оставлен на 190°, тогда как верхняя
      панель ушла на 180°. Сдвиг верхней оказался невидимым, поэтому тащить его
      в нетемизированный компонент заранее смысла нет — решать на живом глазу
- [ ] Роли: активная задача → `ui.fill` + `ui.accent`, свёрнутая →
      `text.tertiary`, требующая внимания → `status.urgent`
- [ ] После этого — удалить мёртвый CSS из `waybar/style.css`
      (`window#waybar.bottom`, `#custom-taskbar` с четырьмя состояниями,
      `#custom-workspace-indicator`, `@define-color bg_panel_bottom`)

---

## Шаг 4 — непокрытые компоненты

Это не дрейф, а дыры: тема их вообще не касается.

- [ ] GTK 3/4 — сейчас Adwaita, `gtk.css` не существует, `GTK_THEME` нигде не задан
- [ ] satty — секция `[color-palette]` не задана, аннотации дефолтные
- [ ] Xresources — нет `color0..15`, XWayland-приложения без палитры
- [ ] SDDM — отдельный репозиторий `dimarch-sddm-theme`
- [ ] Telegram, Firefox — по README

---

## Шаг 5 — документация

- [ ] `dimarch/docs/BRAND.md` — там четвёртая палитра: `panel-bg #16161c`,
      `window-bg #f0ede4`, `text #2a2a35`. Ни одно из значений не встречается
      в компонентах. Обновить после раскатки, а не до.
- [x] `README.md` — переписан вокруг палитры
- [x] `assets/palette.svg` — перегенерирован из `palette.json`

---

## Открытые вопросы

**Генератор.** После того как палитра устоится и линтер покажет реальный объём
ручной синхронизации, станет видно, окупается ли он. Восемь нотаций, прямой
`@import` возможен только у части компонентов (ghostty `config-file`, mpv
`include`, hyprlock `source`, rofi `@import`, CSS `@import`, swayimg `require`);
yazi, herdr, uosc, vscode требуют генерации.

**Правило `G≥B`.** Сформулировано жёстче, чем показала практика: сдвиг фона на
две единицы канала оказался неразличим. Стоит либо смягчить формулировку, либо
признать, что при такой светлоте и насыщенности хюи фона не читается вовсе.

**Именованные иконки yazi.** `dirs`/`exts`/`globs` остались на upstream-дефолтах
с Material-цветами (`.config` оранжевая, `Downloads` бирюзовая). Формально вне
палитры, но переопределение означает переписать сотни записей ради одного поля.

**Светлая тема.** Живёт в Obsidian и в светлом профиле ghostty. `sage-light.css`
содержит полный набор акцентов — понадобится при SDDM и Firefox.
